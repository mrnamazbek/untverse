[project]
name = "unt-informatics-backend"
version = "1.0.0"
description = "UNT Informatics Learning Platform Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
pythonpath = ["."]
testpaths = ["tests"]
